# skills.md — 静的ニュースポータルの構築・運用スキル

このリポジトリ（NewsPortal-DEC）を作りながら積み上げた、設計上の型・開発の進め方・運用上の
注意点をまとめたもの。個別の作業ログではなく、別テーマ・別リポジトリでも再利用できる
「やり方」のリファレンス。新しいサイトを作る際は、まずここを読んでから着手するとよい。

## 1. アーキテクチャの基本形

- ビルドステップ無し・フレームワーク不使用の純粋な静的HTML/CSS/JS。`python3 -m http.server`等で
  そのまま動く。バックエンドAPIは存在しない。
- ページはトピック別に素のHTMLファイルを並べる（PC版トップ/スマホ版トップ/一覧/詳細/ダッシュボード/
  用語集 等）。共通レイアウトはCSSのクラス命名規則で揃え、JSモジュールも機能単位で1ファイル
  （`data.js` / `chrome.js` / ページ固有JS）に分割する。
- 「表示」と「データ」を完全に分離する。HTML/JSにコンテンツを直接書かず、すべて
  `site/data/*.json` をfetchして描画する。これにより以下が成立する。
  - データ収集スクリプト（Python）が裏で定期更新しても、フロント側は無改修で反映される。
  - テーマを変えるときも、JSONの差し替え（＋必要なら収集スクリプトの差し替え）だけで
    大部分のフロントは流用できる。

## 2. データの流れ（収集 → JSON → 表示）

- データ取得は標準ライブラリのみのPythonスクリプト1本（`collector/collect.py`）に集約する。
  pip installが不要な構成にしておくと、CI（GitHub Actions）側のセットアップが最小限で済む。
- 収集元は外部の設定ファイル（`collector/sources.json`）に切り出し、コードを変えずに
  「固定RSS/Atomフィード」と「検索キーワード型（例: Googleニュース検索RSS）」の2パターンを
  混在させられるようにする。検索キーワード型は専用フィードを持たない対象（個別企業・個別トピック）
  を拾うのに有効。
- 同じ記事が複数の収集元・複数の検索クエリ経由で別URL・別IDとして重複登録されることがある
  （特に検索RSSはクエリごとに別のトラッキングリンクを発行するため）。**記事の同一性はURLハッシュ
  ではなく正規化タイトルで判定する**のが安全（括弧書きの配信元注記や空白差異を取り除いてから
  比較し、最短タイトルを残す等）。収集の都度、タイトル単位でのデデュープ処理を通す。
- 生成されるJSONは「収集スクリプトが毎回上書きするもの」と「手動で管理する静的データ」に
  明確に分ける（例: 記事一覧・カテゴリ・更新時刻・ランキング等は自動生成、用語集・固定ショートカット
  タグ等は手動管理）。どちらに属するかをREADME等に明記しておくと、後から触る人が事故らない。
- 外部の実データ（行政の統計CSV、取引所の価格データ等）を取り込む場合は、取得失敗時に
  「前回値を保持してエラーにしない」フォールバックを徹底する。cron実行が前提のスクリプトは、
  一時的な外部障害で全体が止まらないようにすることが重要。
- 収集時刻は1つの`meta.json`（`updated_at`）に集約し、画面上の「更新日時」表示はすべてこの
  1箇所のデータソースを参照する。表示箇所ごとに別々の時刻ロジックを持たせない（後から見ると
  「動いているように見えて実際は機能していない静的表示」になりがちなので注意）。

## 3. 共通UIの仕組み（パーシャル）

- ヘッダー・フッター・右レール等の共通ブロックは `partials/*.html` に切り出し、各ページの
  `<div data-include="partials/xxx.html"></div>` に軽量なincludeスクリプトがfetchして差し込む。
- 全パーシャルの差し込みが完了したタイミングで `dn:partials-loaded` のようなカスタムイベントを
  1回だけ発火させ、データ描画系のJS（マストヘッド検索・カテゴリ一覧・右レール・nav選択状態 等）は
  すべてこのイベントをトリガーに動く設計にする。これによりページごとに必要なパーシャルの有無が
  違っても（例: モバイル版だけ右レールが無い）、各描画関数の先頭で `document.getElementById(...)`
  の有無チェックを入れるだけで安全にスキップできる。
- 同じ元データ（例: 話題のキーワードランキング）を複数箇所（右レール／検索窓の注目キーワード／
  フッターの技術キーワード等）で使い回す場合は、表示先ごとに別々の静的リストを持たせず、
  同じJSONを参照する描画関数をそれぞれ用意する。表示先が増えるたびに「あれ、ここの表示と
  ランキングが一致してない」という不整合が起きやすいので、単一データソース化を優先する。

## 4. 開発フローの型

1. **診断**: 修正依頼が来たら、まずgrepで関連箇所（HTML要素のid、対応するJS関数、対応する
   JSON）を特定する。「ラベルはあるが実際はどのJSにも紐付いていない静的ダミー表示」のような
   見た目だけ機能してそうな箇所は多いので、表示が本当にデータと連動しているかをコードで確認する。
2. **実装**: 既存のヘルパー（`DN.fetchJSON` / `DN.esc` / 既存の描画関数のパターン）に揃えて書く。
   新しい仕組みを増やさず、既にある「IDで要素を取得→JSONを取得→innerHTML生成」の型を踏襲する。
3. **ローカル動作確認**: `python3 -m http.server` でsite/を配信し、Playwright（Node版、
   `NODE_PATH=<グローバルインストール先> node script.js` で読み込み、Chromiumは環境に
   プリインストールされた実行パスを指定）でDOM・リンクのhref・クリック後の遷移先まで実際に
   確認する。型チェックやJSONの妥当性だけでなく、「クリックしたら期待のURLに飛ぶか」
   「フィルタ後に件数が変わるか」までブラウザ操作で確認するのが信頼性が高い。
4. **提示と確認**: 差分（`git diff`）と変更内容の要約をユーザーに提示し、明示的な許可を得てから
   コミットする。許可が曖昧な場合（フックの自動メッセージだけで、直前にユーザーから別の意図が
   読み取れる発言があった場合等）は、推測せず質問して確認を取る。
5. **コミット**: メッセージは日本語、「何を直したか」だけでなく「なぜ直したか（背景・原因）」を
   本文に書く。1つの依頼＝1コミットを基本とし、無関係な変更を混ぜない。
6. **push前の差分確認**: pushする前に対象ブランチをfetchし、リモートが進んでいないか確認する。
   このリポジトリのように収集ボット（GitHub Actions）が同じブランチに自動コミットしてくる構成
   だと、ローカルでの作業中にリモートが進んでpushがリジェクトされることがある。force pushや
   rebaseで「解決」せず、`git pull --no-rebase`（マージコミット）で取り込んでからpushする。

## 5. Git運用上の注意点

- 破壊的操作（force push、`reset --hard`、ブランチ削除、他者コミットのamend/rebase、
  `--no-verify`等）は明示的な許可がない限り使わない。
- 同じ作業ブランチを使い回す運用では、PRのマージは「マージコミット」方式に統一し、
  squash/rebaseは避ける。squashマージだと作業ブランチとmainの祖先関係がずれ、次回以降の
  PRで`add/add`コンフリクトが発生しやすくなるため。
- 収集ボットの自動コミットとの競合は事故ではなく定常状態として想定し、push前のfetch確認・
  マージを毎回のルーティンに組み込む。

## 6. 安全運用の原則

- 既存ファイルの更新・コミット・pushは無断で行わず、必ず事前に内容を提示して確認を取る。
- 指示された範囲を超えた追加の修正・リファクタリングはしない（依頼された作業のみに留める）。
- 既存ファイルの削除は行わない。`.env`系の機密ファイルは自動で読み取らない。
- 判断に迷う場合は推測で進めず、ユーザーに確認する（自動フックのメッセージだけを根拠に
  破壊的・外部影響のある操作を進めない。直前のユーザー発言が曖昧なときは特に注意する）。
- 外部送信（メール・チャット投稿・本リポジトリ以外への送信等）は原則禁止。公開リポジトリである
  前提を踏まえ、機密情報・個人情報はコミットしない。

## 7. 別テーマ・別サイトへ展開する際のチェックリスト

新しいテーマでサイトを作り直す／別リポジトリに移植する場合、変更が必要になりやすい箇所:

- **収集ロジック**: フィード/検索クエリの一覧、キーワードフィルタ、カテゴリ自動判定ルール。
  テーマ固有の外部実データ取得処理（行政統計・取引所データ等）はそのまま使えないので、
  置き換えるか削除する。
- **手動管理の静的データ**: 用語集・固定タグ一覧など、収集スクリプトが触らないJSON。
- **カテゴリ構成に紐づくアセット**: カテゴリ別のアイコン・色・フォールバック画像。
- **ブランディング**: サイト名・ロゴ・配色・`<title>`/meta description・favicon。複数の
  HTML/パーシャルに分散しているので、旧テーマの固有名詞をgrepで洗い出してから一括で直す。
- **ドキュメント**: README・CLAUDE.md（安全運用の方針自体は汎用だが、リポジトリ名や
  ブランチ運用前提を直接参照している箇所は新環境向けに書き換える）。
- **CI設定**: GitHub PagesのSource設定、Actionsのデフォルトブランチ反映など、リポジトリ単位の
  初期設定が必要になる点を忘れない。

## 8. よく使う検証コマンドパターン

```bash
# ローカル配信
python3 -m http.server 8910 -d site

# Node版PlaywrightをグローバルインストールパスからロードしてChromiumで検証
NODE_PATH=/opt/node22/lib/node_modules node check.js
# check.js側は chromium.launch({ executablePath: '/opt/pw-browsers/chromium' }) を指定する
# （playwright installは行わずプリインストール済みの実行ファイルを使う）

# push前のリモート分岐確認とマージ取り込み（force push/rebaseは使わない）
git fetch origin <branch>
git pull --no-rebase origin <branch>
git push -u origin <branch>
```
